<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://yonseiserver.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yonseiserver.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-03-17T06:25:34+00:00</updated><id>https://yonseiserver.github.io/feed.xml</id><title type="html">Yonseistat HPC Cluster</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Tensorflow 실행하기</title><link href="https://yonseiserver.github.io/blog/2025/tf/" rel="alternate" type="text/html" title="Tensorflow 실행하기"/><published>2025-03-11T12:00:00+00:00</published><updated>2025-03-11T12:00:00+00:00</updated><id>https://yonseiserver.github.io/blog/2025/tf</id><content type="html" xml:base="https://yonseiserver.github.io/blog/2025/tf/"><![CDATA[<h1 id="tensorflow-실행하기">Tensorflow 실행하기</h1> <p><a href="/blog/2025/ssh-python/index.html">SSH 접속 및 Python 실행하기</a> 문서의 Step 1, 2, 3을 먼저 숙지하시기 바랍니다. 이 문서는 그 이후의 내용만을 다룹니다.</p> <h2 id="step4-activating-a-conda-environment">Step4. Activating a conda environment</h2> <p><a href="/blog/2025/ssh-python/index.html">SSH 접속 및 Python 실행하기</a> 문서에서 생성한 <code class="language-plaintext highlighter-rouge">Conda environment</code>를 그대로 사용하면서 <code class="language-plaintext highlighter-rouge">cudatoolkit</code>, <code class="language-plaintext highlighter-rouge">tensorflow</code>, <code class="language-plaintext highlighter-rouge">torch</code>를 설치합니다.</p> <p>각 사용자의 <code class="language-plaintext highlighter-rouge">conda environment</code>에 <code class="language-plaintext highlighter-rouge">tensorflow</code>, <code class="language-plaintext highlighter-rouge">torch</code> 뿐 아니라 <code class="language-plaintext highlighter-rouge">cudatoolkit</code>도 따로 설치됩니다.</p> <h3 id="conda-environment-활성화"><code class="language-plaintext highlighter-rouge">Conda environment</code> 활성화</h3> <p>이제 <a href="/blog/2025/ssh-python/index.html">SSH 접속 및 Python 실행하기</a> 문서의 안내를 따라 진행하면 됩니다. 따라서 설명을 생략하고 sbatch script만 제시합니다. <a href="/blog/2025/ssh-python/index.html">SSH 접속 및 Python 실행하기</a> 문서의 sbatch script와의 차이점은, <code class="language-plaintext highlighter-rouge">conda environment</code>를 생성하는 부분은 생략되고, 활성화만 시켜줍니다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=install_tf</span>
<span class="c">#SBATCH --nodes=1</span>
<span class="c">#SBATCH --time=99:59:59</span>
<span class="c">#SBATCH --mem=4gb</span>
<span class="c">#SBATCH --partition=jobs</span>
<span class="c">#SBATCH --nodelist=hpc</span>
<span class="c">#SBATCH --output=install_tf.log</span>
<span class="c">#SBATCH --error=install_tf.err</span>

<span class="c"># Conda 바이너리 경로</span>
<span class="nv">CONDA_BIN_PATH</span><span class="o">=</span>/conda/anaconda3/bin

<span class="c"># 기존 Conda 환경 경로 (testEnv 사용)</span>
<span class="nv">ENV_NAME</span><span class="o">=</span>testEnv
<span class="nv">ENV_PATH</span><span class="o">=</span>/data/home/<span class="si">$(</span><span class="nb">whoami</span><span class="si">)</span>/.conda/envs/<span class="nv">$ENV_NAME</span>

<span class="c"># Conda 환경 활성화</span>
<span class="nb">source</span> <span class="nv">$CONDA_BIN_PATH</span>/activate <span class="nv">$ENV_PATH</span> 

<span class="c"># TensorFlow, PyTorch 및 관련 라이브러리 설치</span>
conda <span class="nb">install</span> <span class="nt">-y</span> <span class="nv">tensorflow</span><span class="o">=</span>2.3.0 <span class="nv">pytorch</span><span class="o">==</span>1.7.1 <span class="nv">torchvision</span><span class="o">==</span>0.8.2 <span class="nv">torchaudio</span><span class="o">==</span>0.7.2 <span class="nv">cudatoolkit</span><span class="o">=</span>10.1 <span class="nt">-c</span> pytorch 
</code></pre></div></div> <p>cudatoolkit, cudnn 등이 용량이 커서 시간이 조금 오래 걸립니다.</p> <ul> <li> <p><code class="language-plaintext highlighter-rouge">#SBATCH –time=99:59:59</code> 은 작업 시간 최대 허용치를 의미합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">#SBATCH –output</code>, <code class="language-plaintext highlighter-rouge">#SBATCH –error</code>를 원하는 경로와 파일명으로 변경합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">conda install -y</code> 뒤에 설치를 원하는 패키지명을 입력합니다.</p> </li> </ul> <p>위 내용에 따라 job script를 알맞게 수정하여 <code class="language-plaintext highlighter-rouge">Visual Studio Code</code>에서 작성한 뒤, 클러스터 내 <code class="language-plaintext highlighter-rouge">user home directory</code>에 <code class="language-plaintext highlighter-rouge">[your_script_name].job</code>으로 저장합니다(e.g. <code class="language-plaintext highlighter-rouge">install_tf.job</code>).</p> <h2 id="step5-job-script-제출">Step5. job script 제출</h2> <h3 id="step51-python-코드-작성">Step5.1 Python 코드 작성</h3> <p>이제 클러스터에서 실행할 <code class="language-plaintext highlighter-rouge">Python</code> 코드를 <code class="language-plaintext highlighter-rouge">local</code>이나 <code class="language-plaintext highlighter-rouge">Jupyter</code>에서 작성하고 오류 없이 돌아가는지 확인합니다. 그 후 클러스터의 <code class="language-plaintext highlighter-rouge">user home directory</code>에 옮기거나, <code class="language-plaintext highlighter-rouge">Visual Studio Code</code>내에서 작성하여 저장합니다.</p> <p>아래는 <code class="language-plaintext highlighter-rouge">TensorFlow</code> 공식 페이지에 게시된 초보자용 문서 코드입니다. Batch script를 작성할 때는 알고리즘의 output이 자동으로 저장되지 않으므로 파일로 결과를 저장하는 코드를 포함하는 것이 좋습니다. 아래 코드에는 결과를 저장하는 코드는 없지만, <code class="language-plaintext highlighter-rouge">tensorflow</code>가 학습 과정을 콘솔에 출력하기 때문에 이를 로그 파일에서 볼 수 있습니다. 아래 코드를 <code class="language-plaintext highlighter-rouge">tensor.py</code>라는 이름으로 <code class="language-plaintext highlighter-rouge">user home directory</code>에 저장합니다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># tensor.py
</span>
<span class="kn">import</span> <span class="n">tensorflow</span> <span class="k">as</span> <span class="n">tf</span>

<span class="n">mnist</span> <span class="o">=</span> <span class="n">tf</span><span class="p">.</span><span class="n">keras</span><span class="p">.</span><span class="n">datasets</span><span class="p">.</span><span class="n">mnist</span>

<span class="p">(</span><span class="n">x_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">),</span> <span class="p">(</span><span class="n">x_test</span><span class="p">,</span> <span class="n">y_test</span><span class="p">)</span> <span class="o">=</span> <span class="n">mnist</span><span class="p">.</span><span class="nf">load_data</span><span class="p">()</span>
<span class="n">x_train</span><span class="p">,</span> <span class="n">x_test</span> <span class="o">=</span> <span class="n">x_train</span> <span class="o">/</span> <span class="mf">255.0</span><span class="p">,</span> <span class="n">x_test</span> <span class="o">/</span> <span class="mf">255.0</span>

<span class="n">model</span> <span class="o">=</span> <span class="n">tf</span><span class="p">.</span><span class="n">keras</span><span class="p">.</span><span class="n">models</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">([</span>
  <span class="n">tf</span><span class="p">.</span><span class="n">keras</span><span class="p">.</span><span class="n">layers</span><span class="p">.</span><span class="nc">Flatten</span><span class="p">(</span><span class="n">input_shape</span><span class="o">=</span><span class="p">(</span><span class="mi">28</span><span class="p">,</span> <span class="mi">28</span><span class="p">)),</span>
  <span class="n">tf</span><span class="p">.</span><span class="n">keras</span><span class="p">.</span><span class="n">layers</span><span class="p">.</span><span class="nc">Dense</span><span class="p">(</span><span class="mi">128</span><span class="p">,</span> <span class="n">activation</span><span class="o">=</span><span class="sh">'</span><span class="s">relu</span><span class="sh">'</span><span class="p">),</span>
  <span class="n">tf</span><span class="p">.</span><span class="n">keras</span><span class="p">.</span><span class="n">layers</span><span class="p">.</span><span class="nc">Dropout</span><span class="p">(</span><span class="mf">0.2</span><span class="p">),</span>
  <span class="n">tf</span><span class="p">.</span><span class="n">keras</span><span class="p">.</span><span class="n">layers</span><span class="p">.</span><span class="nc">Dense</span><span class="p">(</span><span class="mi">10</span><span class="p">,</span> <span class="n">activation</span><span class="o">=</span><span class="sh">'</span><span class="s">softmax</span><span class="sh">'</span><span class="p">)</span>
<span class="p">])</span>

<span class="n">model</span><span class="p">.</span><span class="nf">compile</span><span class="p">(</span><span class="n">optimizer</span><span class="o">=</span><span class="sh">'</span><span class="s">adam</span><span class="sh">'</span><span class="p">,</span>
              <span class="n">loss</span><span class="o">=</span><span class="sh">'</span><span class="s">sparse_categorical_crossentropy</span><span class="sh">'</span><span class="p">,</span>
              <span class="n">metrics</span><span class="o">=</span><span class="p">[</span><span class="sh">'</span><span class="s">accuracy</span><span class="sh">'</span><span class="p">])</span>

<span class="n">model</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">x_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">epochs</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>

<span class="n">model</span><span class="p">.</span><span class="nf">evaluate</span><span class="p">(</span><span class="n">x_test</span><span class="p">,</span>  <span class="n">y_test</span><span class="p">,</span> <span class="n">verbose</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <h3 id="step53-slurm-batch-script-작성">Step5.3 Slurm batch script 작성</h3> <p>앞선 단계에서 만든 해당 <code class="language-plaintext highlighter-rouge">conda environment</code>를 activate하고 코드를 실행하는 Slurm batch script를 작성합니다. 클러스터 소개 페이지의 <code class="language-plaintext highlighter-rouge">slurm job configurator</code>를 사용하면 script를 쉽게 작성할 수 있습니다.</p> <p><img src="/assets/img/img22.jpg" alt="이미지22"/></p> <ul> <li> <p><code class="language-plaintext highlighter-rouge">Conda activate</code>에 체크합니다.</p> </li> <li> <p>빈칸들을 채웁니다.</p> </li> <li> <p>Script란에 <code class="language-plaintext highlighter-rouge">python xxx.py</code>라고 작성합니다. 이는 <code class="language-plaintext highlighter-rouge">home directory</code>에 있는 <code class="language-plaintext highlighter-rouge">xxx.py</code> 파일을 Python으로 실행하라는 의미입니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">Print &amp; Copy</code> 버튼을 누르면 내용이 클립보드에 복사됩니다.</p> </li> </ul> <p>이 문서에서 사용한 Slurm batch script의 내용은 아래와 같습니다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash </span>
<span class="c">#</span>
<span class="c">#SBATCH --job-name=tensor_hpc</span>
<span class="c">#SBATCH --partition=jobs</span>
<span class="c">#SBATCH --account=testuser</span>
<span class="c">#SBATCH --mem=8gb</span>
<span class="c">#SBATCH --ntasks=1</span>
<span class="c">#SBATCH --cpus-per-task=8</span>
<span class="c">#SBATCH --time=00:30:00</span>
<span class="c">#SBATCH --output=/data/home/testuser/tensor_hpc.log</span>
<span class="c">#SBATCH --error=/data/home/testuser/tensor_hpc.err</span>
<span class="c">#SBATCH --nodelist=hpc</span>

<span class="nv">CONDA_BIN_PATH</span><span class="o">=</span>/conda/anaconda3/bin
<span class="nv">ENV_NAME</span><span class="o">=</span>testEnv
<span class="nv">ENV_PATH</span><span class="o">=</span>/data/home/testuser/.conda/envs/<span class="nv">$ENV_NAME</span>
<span class="nb">source</span> <span class="nv">$CONDA_BIN_PATH</span>/activate <span class="nv">$ENV_PATH</span> 

python tensor.py
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">tensor_hpc.job</code> 이라는 이름으로 <code class="language-plaintext highlighter-rouge">user home directory</code>에 저장합니다.</p> <p>sbatch에 대한 더 자세한 정보는 <a href="https://slurm.schedmd.com/sbatch.html">Slurm 공식 웹페이지</a>를 참조하세요.</p> <h3 id="step54-slurm-batch-script-실행">Step5.4 Slurm batch script 실행</h3> <p><code class="language-plaintext highlighter-rouge">Conda environment</code>를 만들 때처럼, <code class="language-plaintext highlighter-rouge">sbatch</code> 커맨드를 통해 job을 제출합니다. 할당되는 job 번호는 나중에 <code class="language-plaintext highlighter-rouge">squeue</code>를 통해 정보를 확인하거나 job을 취소할 때 이용되므로 기록해 놓아야 합니다.</p> <p>터미널을 여러 개 띄운 다음 <code class="language-plaintext highlighter-rouge">watch -n 10 -d squeue</code>로 작업 현황을 확인하고, <code class="language-plaintext highlighter-rouge">cat xxx.log</code>이나 <code class="language-plaintext highlighter-rouge">tail -f xxx.err</code>으로 콘솔 출력이나 error를 확인합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sbatch tensor.job
watch -n 10 -d squeue # 작업 현황을 10초마다 갱신하여 보여줍니다. ctrl+c로 escape 할 수 있습니다.
cat tensor.log
tail -f tensor.err
</code></pre></div></div> <p>로그 파일에 콘솔 아웃풋이 기록됩니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1847/1875 [============================&gt;.] - ETA: 0s - loss: 0.0738 - accuracy: 0.9769
1862/1875 [============================&gt;.] - ETA: 0s - loss: 0.0741 - accuracy: 0.9767
1875/1875 [==============================] - 6s 3ms/step - loss: 0.0741 - accuracy: 0.9767
313/313 - 1s - loss: 0.0742 - accuracy: 0.9777
</code></pre></div></div> <h1 id="더-알아보기">더 알아보기</h1> <p><a href="https://doc.zih.tu-dresden.de/jobs_and_resources/slurm_examples/">Slurm Job Examples</a></p> <p><a href="https://researchcomputing.princeton.edu/support/knowledge-base/tensorflow">Tensorflow on the HPC Cluster</a></p>]]></content><author><name></name></author><category term="Tensorflow"/><summary type="html"><![CDATA[HPC에서 Tensorflow 실행 방법]]></summary></entry><entry><title type="html">R 실행하기</title><link href="https://yonseiserver.github.io/blog/2025/r/" rel="alternate" type="text/html" title="R 실행하기"/><published>2025-03-10T13:00:00+00:00</published><updated>2025-03-10T13:00:00+00:00</updated><id>https://yonseiserver.github.io/blog/2025/r</id><content type="html" xml:base="https://yonseiserver.github.io/blog/2025/r/"><![CDATA[<h1 id="r-실행하기">R 실행하기</h1> <p><a href="/blog/2025/ssh-python/index.html">SSH 접속 및 Python 실행하기</a> 문서의 Step 1, 2, 3을 먼저 숙지하시기 바랍니다. 이 문서는 그 이후의 내용만을 다룹니다.</p> <h2 id="step4-필요한-r-패키지를-자신의-디렉토리에-설치하기">Step4. 필요한 R 패키지를 자신의 디렉토리에 설치하기</h2> <p><code class="language-plaintext highlighter-rouge">R</code>은 <code class="language-plaintext highlighter-rouge">conda environment</code>를 사용하지 않으며, <code class="language-plaintext highlighter-rouge">R</code> 패키지들은 <code class="language-plaintext highlighter-rouge">install.packages</code>를 통해 설치할 때 별도의 옵션을 주지 않으면 user별 directory가 아닌 user 폴더 내의 site-library 폴더에 저장됩니다.</p> <p>먼저, 디렉토리 관리의 용이성을 위하여 <code class="language-plaintext highlighter-rouge">User home directory</code> 밑에 <code class="language-plaintext highlighter-rouge">R_packages</code> 디렉토리를 생성해줍니다. 터미널에 다음과 같은 코드를 입력하면 <code class="language-plaintext highlighter-rouge">data/home/testuser/R_packages</code> 디렉토리가 생성됩니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mkdir -p /data/home/$(whoami)/R_packages
</code></pre></div></div> <p>아래의 샘플 코드는 R 패키지를 <code class="language-plaintext highlighter-rouge">data/home/testuser/R_packages</code> 라는 디렉토리에 설치합니다. <strong>install.packages()</strong>에서</p> <ul> <li> <p><code class="language-plaintext highlighter-rouge">lib</code> 옵션을 통해 자신의 개인 디렉토리에 패키지를 설치할 수 있습니다. 이 옵션을 사용하지 않으면 사용자 간에 패키지 버전이 충돌할 수 있으므로 이 옵션을 사용하는 것을 강력히 권장합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">force = FALSE</code> 옵션은 이미 설치된 패키지를 또 설치하는 것을 막아줍니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">INSTALL_opts = c('–no-lock')</code> 옵션은 설치가 이전에 강제로 중단되어서 directory에 락이 걸렸을 때 락을 무시하고 설치하게 합니다.</p> </li> </ul> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lib_r_packages = "/data/home/$(whoami)/R_packages"
install.packages(
"WeightSVM",
lib = lib_r_packages,
dependencies = TRUE,
force = FALSE,
INSTALL_opts = c('--no-lock')
)
install.packages(
"e1071",
lib = lib_r_packages,
dependencies = TRUE,
force = FALSE,
INSTALL_opts = c('--no-lock')
)
install.packages(
"mclust",
lib = lib_r_packages,
dependencies = TRUE,
force = FALSE,
INSTALL_opts = c('--no-lock')
)
install.packages(
"caret",
lib = lib_r_packages,
dependencies = TRUE,
force = FALSE,
INSTALL_opts = c('--no-lock')
)
</code></pre></div></div> <p>위 코드를 <code class="language-plaintext highlighter-rouge">User home directory</code>에 <code class="language-plaintext highlighter-rouge">install_R_packages.R</code>로 저장합니다. 이제 이 설치 코드를 실행하는 slurm job script를 <a href="#step7-slurm-batch-script-작성">Step7</a>을 참고해 생성한 다음 <code class="language-plaintext highlighter-rouge">sbatch</code>로 실행합니다.</p> <h2 id="step5-r-코드-작성">Step5. R 코드 작성</h2> <p>클러스터에서 실행할 <code class="language-plaintext highlighter-rouge">R</code> 코드를 local에서 작성합니다. 코드가 문제 없이 실행되는지 먼저 local에서 확인합니다. 그 후 실제로 실행할 코드를 작성하여 클러스터의 <code class="language-plaintext highlighter-rouge">User home directory</code>에 옮기거나, <code class="language-plaintext highlighter-rouge">Visual Studio Code</code>내에서 작성하여 저장합니다.</p> <p>아래의 샘플 코드는 <code class="language-plaintext highlighter-rouge">xgboost</code>와 <code class="language-plaintext highlighter-rouge">caret</code>을 개인 디렉토리에 설치 및 로드하고 learning과 prediction을 수행한 다음 prediction 결과 plot을 jpeg로 저장하는 코드입니다. <code class="language-plaintext highlighter-rouge">library(xgboost, lib.loc = lib_r_packages)</code> 에서 <code class="language-plaintext highlighter-rouge">lib.loc</code> 옵션이 개인 디렉토리에 설치된 패키지를 로드하는 옵션입니다. Batch script를 작성할 때는 알고리즘의 output이 자동으로 저장되지 않으므로 파일로 결과를 저장하는 코드를 포함하는 것이 좋습니다. 아래 코드를 <code class="language-plaintext highlighter-rouge">R_test_hpc.R</code>로 저장하여 <code class="language-plaintext highlighter-rouge">User home directory</code>에 둡니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lib_r_packages = "/data/home/$(whoami)/R_packages"

install.packages("xgboost",
  lib = lib_r_packages,
  dependencies = TRUE,
  force = FALSE,
  INSTALL_opts = c('--no-lock')
  )

install.packages("caret",
  lib = lib_r_packages,
  dependencies = TRUE,
  force = FALSE,
  INSTALL_opts = c('--no-lock')
  )

library(withr, lib.loc = lib_r_packages)
library(xgboost, lib.loc = lib_r_packages)
library(caret, lib.loc = lib_r_packages)

boston = MASS::Boston
str(boston)

set.seed(12)
indexes = createDataPartition(boston$medv, p = .85, list = F)
train = boston[indexes, ]
test = boston[-indexes, ]

train_x = data.matrix(train[, -13])
train_y = train[,13]

test_x = data.matrix(test[, -13])
test_y = test[, 13]

xgb_train = xgb.DMatrix(data = train_x, label = train_y)
xgb_test = xgb.DMatrix(data = test_x, label = test_y)

xgbc = xgboost(data = xgb_train, max.depth = 2, nrounds = 50)
print(xgbc)

pred_y = predict(xgbc, xgb_test)

mse = mean((test_y - pred_y)^2)
mae = caret::MAE(test_y, pred_y)
rmse = caret::RMSE(test_y, pred_y)

cat("MSE: ", mse, "MAE: ", mae, " RMSE: ", rmse)

x = 1:length(test_y)
jpeg(file="R_plot.jpeg")
plot(x, test_y, col = "red", type = "l")
lines(x, pred_y, col = "blue", type = "l")
legend(x = 1, y = 38,  legend = c("original test_y", "predicted test_y"), 
       col = c("red", "blue"), box.lty = 1, cex = 0.8, lty = c(1, 1))
dev.off()
</code></pre></div></div> <h2 id="step6-현재-클러스터-자원-사용량-확인">Step6. 현재 클러스터 자원 사용량 확인</h2> <p>아래 커맨드를 통해 해당 노드의 cpu와 RAM 사용 현황을 볼 수 있습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sinfo -o "%n %e %m %a %c %C"
</code></pre></div></div> <p>아래와 같은 결과가 나옵니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HOSTNAMES FREE_MEM MEMORY AVAIL CPUS CPUS(A/I/O/T)
hpc 215880 257613 up 64 0/64/0/64
</code></pre></div></div> <ul> <li> <p>CPUS의 A/I/O/T는 allocated/idle/other/total을 의미합니다.</p> </li> <li> <p>자신의 job이 바로 실행되기를 원한다면, Slurm batch script를 작성할 때</p> <ul> <li> <p>RAM 용량을 FREE_MEM보다 적게 설정해야 합니다.</p> </li> <li> <p>CPU 코어 개수를 CPUS idle보다 적게 설정해야 합니다.</p> </li> <li> <p>실제 필요 이상의 자원을 올리면 다른 사용자의 사용에 어려움이 있을 수 있으니 반드시 필요한 만큼만 올려야 합니다.</p> </li> </ul> </li> <li> <p>현재 가용 자원보다 더 많은 자원을 요구하는 script를 작성하면, job이 바로 실행되지 않습니다. 대기 상태에 있다가 다른 user들의 job이 끝나고 자원이 반환되면 job이 실행됩니다.</p> </li> </ul> <h2 id="step7-slurm-batch-script-작성">Step7. Slurm batch script 작성</h2> <p>작성한 코드를 해당 node에서 실행하는 Slurm batch script를 작성합니다. 클러스터 소개 페이지의 slurm job configurator를 사용하면 script를 쉽게 작성할 수 있습니다.</p> <p><img src="/assets/img/img20.jpg" alt="이미지20"/></p> <ul> <li> <p><code class="language-plaintext highlighter-rouge">Python</code>과 달리 conda environment를 사용하지 않으므로, <code class="language-plaintext highlighter-rouge">conda activate</code>에 체크하지 않습니다.</p> </li> <li> <p>빈칸들을 채웁니다.</p> </li> <li> <p>Script란에 <code class="language-plaintext highlighter-rouge">Rscript xxx.R</code>라고 작성합니다. 이는 <code class="language-plaintext highlighter-rouge">home directory</code>에 있는 <code class="language-plaintext highlighter-rouge">xxx.R</code> 파일을 <code class="language-plaintext highlighter-rouge">R</code>로 실행하라는 의미입니다. Job script를 작성하거나 <code class="language-plaintext highlighter-rouge">sbatch</code> 명령어를 사용할 때, <code class="language-plaintext highlighter-rouge">visual studio code</code>의 explorer에서 파일명을 마우스 우클릭하고 경로 복사를 사용하면 편리합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">Print &amp; Copy</code> 버튼을 누르면 내용이 클립보드에 복사됩니다.</p> </li> </ul> <p><code class="language-plaintext highlighter-rouge">R_test_hpc.job</code>이라는 이름으로 클러스터의 <code class="language-plaintext highlighter-rouge">user home directory</code>에 저장합니다.</p> <p>Configurator로 생성한 Slurm batch script의 내용은 아래와 같습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#!/bin/bash 
#
#SBATCH --job-name=R_test_hpc
#SBATCH --partition=jobs
#SBATCH --account=testuser
#SBATCH --mem=16gb
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=4
#SBATCH --time=00:50:00
#SBATCH --output=/data/home/testuser/R_test_hpc.log
#SBATCH --error=/data/home/testuser/R_test_hpc.err
#SBATCH --nodelist=hpc

Rscript R_test_hpc.R
</code></pre></div></div> <p>Script 윗부분의 #SBATCH 옵션들의 의미는 다음과 같습니다.</p> <ul> <li> <p><code class="language-plaintext highlighter-rouge">--job-name</code>: 수행할 작업의 이름</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--mem</code>: memory limit</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--nodelist</code>: 작업할 노드의 이름</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--ntasks</code>: 작업의 수</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--cpus-per-task</code>: 각 작업에서 사용할 cpu 코어의 수</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--time</code>: 작업 제한시간</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--account</code>: 해당 작업을 수행하는 계정의 이름</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--partition</code>: group of nodes with specific characteristics</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--nodelist</code>: 사용할 node의 이름</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--output</code>: 코드 실행 결과 log 파일. 확장자는 out이나 log가 가능합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--error</code>: 코드 실행 결과 log error 파일과 log 파일의 파일명과 저장 경로는 원하는 데로 수정할 수 있습니다. sbatch에 대한 더 자세한 정보는 <a href="https://slurm.schedmd.com/sbatch.html">Slurm 공식 웹페이지</a>를 참조하세요.</p> </li> </ul> <h2 id="step8-slurm-batch-script-실행">Step8. Slurm batch script 실행</h2> <p><code class="language-plaintext highlighter-rouge">sbatch</code> 커맨드를 통해 job을 제출합니다. <a href="/blog/2025/ssh-python/index.html">SSH 접속 및 Python 실행하기</a> 문서의 Step 4에서처럼, <code class="language-plaintext highlighter-rouge">ctrl+shift+~</code>를 눌러 터미널을 여러 개 띄우고 <code class="language-plaintext highlighter-rouge">watch -n 10 -d squeue</code>로 작업 현황을 확인하고, <code class="language-plaintext highlighter-rouge">tail -f xxx.out</code>, <code class="language-plaintext highlighter-rouge">tail -f xxx.err</code>으로 콘솔 출력이나 error를 확인합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sbatch R_test_hpc.job
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">User home directory</code>에 <code class="language-plaintext highlighter-rouge">R_plot.jpeg</code>가 다음과 같이 생성되면 작업이 완료된 것입니다.</p> <p><img src="/assets/img/img21.jpg" alt="이미지21"/></p> <p>만약 해당 이미지 생성이 되지 않았다면, r package 일부가 제대로 설치되지 않은 것입니다.</p> <p>err 파일을 열어 설치 되지 않은 r package를 확인한 후 터미널에서 R 커맨드를 열어 수동으로 설치하시기 바랍니다.</p> <h1 id="더-알아보기">더 알아보기</h1> <p><a href="https://doc.zih.tu-dresden.de/jobs_and_resources/slurm_examples/">SLURM Job Examples</a></p>]]></content><author><name></name></author><category term="R"/><summary type="html"><![CDATA[HPC에서 R 실행 방법]]></summary></entry><entry><title type="html">SSH 접속 및 Python 실행하기</title><link href="https://yonseiserver.github.io/blog/2025/ssh-python/" rel="alternate" type="text/html" title="SSH 접속 및 Python 실행하기"/><published>2025-03-10T12:00:00+00:00</published><updated>2025-03-10T12:00:00+00:00</updated><id>https://yonseiserver.github.io/blog/2025/ssh-python</id><content type="html" xml:base="https://yonseiserver.github.io/blog/2025/ssh-python/"><![CDATA[<p>R 사용자는 Step1-3을 숙지한 뒤 <a href="/blog/2025/r/index.html">R 문서</a>로 넘어가세요.</p> <h2 id="step-1-terminal-앱-고르기">Step 1. terminal 앱 고르기</h2> <p>User는 <code class="language-plaintext highlighter-rouge">SSH</code>로 <code class="language-plaintext highlighter-rouge">hpc node</code>에 접속하여 클러스터를 사용합니다. 터미널 환경과 <code class="language-plaintext highlighter-rouge">vi</code> 에디터에 익숙한 user는 자신에게 친숙한 앱을 사용하면 됩니다. 그렇지 않은 경우 <code class="language-plaintext highlighter-rouge">Visual Studio Code</code>를 사용하는 것을 추천합니다. 이 문서에는 <code class="language-plaintext highlighter-rouge">Visual Studio Code</code>를 사용하는 것을 전제로 합니다. 추천 이유는 다음과 같습니다.</p> <ul> <li> <p>Windows, MacOS, Linux에서 모두 사용 가능합니다.</p> </li> <li> <p>터미널과 에디터, 파일 브라우저가 통합되어 있습니다.</p> <ul> <li> <p>불편하게 vi나 nano등의 CLI용 텍스트 에디터를 사용할 필요가 없습니다.</p> </li> <li> <p>파일 전송시 scp등의 복잡한 프로토콜을 사용하지 않고 drag &amp; drop으로 수행할 수 있습니다.</p> </li> </ul> </li> </ul> <h2 id="step-2-ssh-접속">Step 2. SSH 접속</h2> <p><code class="language-plaintext highlighter-rouge">Visual Studio Code</code>를 설치하고 아래 안내에 따라 설정합니다.</p> <h3 id="1-visual-studio-code-extensions에서-remote-development-설치">1. Visual Studio Code extensions에서 Remote Development 설치</h3> <p>Microsoft가 제공하는 <code class="language-plaintext highlighter-rouge">Remote Development</code> extension pack을 설치합니다. WSL, Dev Containers, Remote-SSH, Remote-Tunnels가 자동적으로 같이 설치됩니다.</p> <p><img src="/assets/img/img1.jpg" alt="이미지1"/></p> <p><img src="/assets/img/img2.jpg" alt="이미지2"/></p> <h3 id="2-remote-explorer에서-remotestunnelsssh를-선택-후--클릭">2. Remote Explorer에서 REMOTES(TUNNELS/SSH)를 선택 후, + 클릭</h3> <p><img src="/assets/img/img3.jpg" alt="이미지3"/></p> <h3 id="3-ssh-접속-커맨드-입력">3. SSH 접속 커맨드 입력</h3> <p>아래와 같은 창이 뜨면 <code class="language-plaintext highlighter-rouge">SSH</code> 커맨드를 입력하여 <code class="language-plaintext highlighter-rouge">hpc node</code>에 접속합니다.</p> <p><img src="/assets/img/img4.jpg" alt="이미지4"/></p> <p>아래 커맨드에 Slack으로 안내받은 port, username, <code class="language-plaintext highlighter-rouge">hpc node</code>의 ip을 넣어서 위 창에 입력하고 <code class="language-plaintext highlighter-rouge">Enter</code>키를 누르면 됩니다. <code class="language-plaintext highlighter-rouge">SSH</code>의 default port는 <code class="language-plaintext highlighter-rouge">22</code>이지만, 저희는 보안상 이유로 다른 port를 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh -p [port] [username]@[ip]
</code></pre></div></div> <h3 id="4-ssh-configuration-file을-저장할-장소-선택">4. SSH configuration file을 저장할 장소 선택</h3> <p><strong>Select SSH configuration file to update</strong>가 나오면 맨 위 항목을 선택합니다.</p> <p><img src="/assets/img/img5.jpg" alt="이미지5"/></p> <p><strong>Host added!</strong> 라는 메시지가 우측 하단에 나옵니다.</p> <p><img src="/assets/img/img6.jpg" alt="이미지6"/></p> <h3 id="5-host명-수정선택사항">5. Host명 수정(선택사항)</h3> <p>초기 Host명이 <code class="language-plaintext highlighter-rouge">hpc node</code>의 ip주소로 설정됩니다.(xxx.xxx.xx.xx 형식) 사용자가 Host명을 명명하고 싶다면 다음 과정을 수행합니다.</p> <p>Remote Explorer에서 SSH 옆 톱니바퀴를 클릭합니다.</p> <p><img src="/assets/img/img7.jpg" alt="이미지7"/></p> <p>Select SSH configuration file to update가 나오면 맨 위 항목을 선택합니다.</p> <p><img src="/assets/img/img8.jpg" alt="이미지8"/></p> <p>다음과 같이 config 파일이 열립니다. (아래는 ip주소와 포트번호 노출을 막기 위해 Host, HostName, Port 옆을 수정한 이미지입니다. config 파일 여시면 ip주소와 포트번호 모두 안내받으신 대로 보이는 것이 정상입니다.)</p> <p><img src="/assets/img/img9.jpg" alt="이미지9"/></p> <p>Host 옆 내용을 사용자 임의로 수정한 후 해당 config 파일을 저장합니다. 여기에서는 yonsihpc로 수정해 주었습니다. Host 옆 이외의 부분은 수정하지 않습니다.</p> <p><img src="/assets/img/img10.jpg" alt="이미지10"/></p> <p>다음과 같이 Host명이 수정된 것을 Remote Explorer에서 확인할 수 있습니다.</p> <p><img src="/assets/img/img11.jpg" alt="이미지11"/></p> <h3 id="6-remote-explorer에서-connect-to-host-in-new-window-선택">6. Remote Explorer에서 Connect to Host in New Window 선택</h3> <p><img src="/assets/img/img12.jpg" alt="이미지12"/></p> <h3 id="7-서버-platform-선택">7. 서버 Platform 선택</h3> <p><strong>Linux</strong>를 선택합니다.</p> <p><img src="/assets/img/img13.jpg" alt="이미지13"/></p> <h3 id="8-password-입력">8. Password 입력</h3> <p>안내받은 password를 입력하여 로그인합니다.</p> <p><img src="/assets/img/img14.jpg" alt="이미지14"/></p> <h3 id="9-파일-시스템-마운트">9. 파일 시스템 마운트</h3> <p>좌측 탭의 파일 모양 아이콘을 클릭하고 <strong>Open Folder</strong> 버튼을 클릭합니다.</p> <p><img src="/assets/img/img15.jpg" alt="이미지15"/></p> <p>기본적으로 user home directory 경로가 입력되어 있습니다. OK를 누릅니다.</p> <p><img src="/assets/img/img16.jpg" alt="이미지16"/></p> <p>다시 password 입력창이 뜬다면, 안내받은 password를 입력합니다.</p> <p><img src="/assets/img/img14.jpg" alt="이미지17"/></p> <h3 id="10-둘러보기">10. 둘러보기</h3> <p><img src="/assets/img/img18.jpg" alt="이미지18"/></p> <ul> <li> <p>좌측 file explorer에서 파일을 관리합니다. Windows 탐색기나 MacOS Finder에서 drag&amp;drop으로 파일을 옮길 수 있습니다. 클러스터 내부의 파일을 user의 local 컴퓨터로 가져오는 것도 drag&amp;drop으로 가능합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">ctrl + shift + ~</code>키를 누르면 터미널이 열립니다. 여기서 서버 사용에 필요한 커맨드를 입력합니다. 터미널은 여러 개 띄울 수 있습니다.</p> </li> <li> <p>text editor에서 코드와 스크립트를 수정하고 이미지 파일 등을 열람합니다.</p> </li> </ul> <h3 id="11-user-password-변경">11. user password 변경</h3> <p>모든 user의 초기 password가 다 동일하기 때문에, 각 user는 첫 접속 시 password를 변경할 것을 권장합니다. 터미널에서 아래 커맨드를 입력하여 password를 변경합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>passwd
</code></pre></div></div> <h2 id="step-3-파일-시스템-구조-이해">Step 3. 파일 시스템 구조 이해</h2> <p>User명은 컴퓨팅 클러스터 사용 신청시 제출하신 이메일 주소의 @ 앞 부분과 동일합니다.</p> <p><code class="language-plaintext highlighter-rouge">User home directory</code>의 prefix는 <code class="language-plaintext highlighter-rouge">/data/home/</code>입니다. 예를 들어, <code class="language-plaintext highlighter-rouge">dummyuser</code>라는 <code class="language-plaintext highlighter-rouge">user</code>의 <code class="language-plaintext highlighter-rouge">home directory</code>의 경로는 <code class="language-plaintext highlighter-rouge">/data/home/dummyuser/</code>입니다. 다른 <code class="language-plaintext highlighter-rouge">user의 home directory</code>를 열람할 수 없도록 권한설정이 되어 있습니다. 각 <code class="language-plaintext highlighter-rouge">user</code>는 데이터와 코드, 설정 파일 등을 자신의 <code class="language-plaintext highlighter-rouge">home directory</code> 내에 저장합니다.</p> <ul> <li> <p><code class="language-plaintext highlighter-rouge">Linux</code>에서 <code class="language-plaintext highlighter-rouge">directory</code>를 이동하는 명령어는 <code class="language-plaintext highlighter-rouge">cd</code>입니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">home directory</code>를 나타내는 기호는 <code class="language-plaintext highlighter-rouge">~</code>입니다.</p> </li> <li> <p>현재 <code class="language-plaintext highlighter-rouge">directory</code>를 확인하는 명령어는 <code class="language-plaintext highlighter-rouge">pwd</code>입니다.</p> </li> <li> <p>파일 목록을 확인하는 명령어는 <code class="language-plaintext highlighter-rouge">ls</code>입니다.</p> </li> </ul> <p>따라서 <code class="language-plaintext highlighter-rouge">user</code>는 <code class="language-plaintext highlighter-rouge">hpc node</code>아래의 명령어를 통해 자신의 <code class="language-plaintext highlighter-rouge">home directory</code>로 이동해 그 안에 있는 파일 목록을 확인할 수 있습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cd ~    
ls -a
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>testuser@hpc:~$ ls -a
.  ..  .bash_logout  .bashrc  .cache  .config  .profile  .vscode-server  .wget-hsts
</code></pre></div></div> <h2 id="step-4-conda-environment-생성">Step 4. Conda environment 생성</h2> <ul> <li><code class="language-plaintext highlighter-rouge">hpc node</code>에는 conda 24.9.2이 설치되어 있으며 Python version을 3.12.7까지 지원합니다.</li> </ul> <h3 id="step-41-conda-environment-생성-batch-script-작성">Step 4.1 Conda environment 생성 batch script 작성</h3> <p>여기서는 <code class="language-plaintext highlighter-rouge">hpc node</code>에서 <code class="language-plaintext highlighter-rouge">conda environment</code>를 생성하는 방법을 설명합니다. <code class="language-plaintext highlighter-rouge">Conda environment</code> 생성 slurm batch script를 작성합니다. 아래는 script 예시입니다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --nodes=1</span>
<span class="c">#SBATCH --time=99:59:59</span>
<span class="c">#SBATCH --mem=4gb</span>
<span class="c">#SBATCH --partition=jobs</span>
<span class="c">#SBATCH --nodelist=hpc</span>
<span class="c">#SBATCH --output=testEnv.log</span>
<span class="c">#SBATCH --error=testEnv.err</span>

<span class="c"># Conda 기본 경로 설정</span>
<span class="nv">CONDA_ROOT</span><span class="o">=</span>/conda/anaconda3
<span class="nv">CONDA_BIN_PATH</span><span class="o">=</span><span class="nv">$CONDA_ROOT</span>/bin
<span class="nv">ENV_NAME</span><span class="o">=</span>testEnv
<span class="nv">ENV_PATH</span><span class="o">=</span>/data/home/<span class="si">$(</span><span class="nb">whoami</span><span class="si">)</span>/.conda/envs/<span class="nv">$ENV_NAME</span>  <span class="c"># 사용자 경로 수정</span>

<span class="c"># Conda 환경 확인 (디버깅용)</span>
<span class="nb">echo</span> <span class="s2">"Conda 위치: </span><span class="si">$(</span>which conda<span class="si">)</span><span class="s2">"</span>
conda <span class="nt">--version</span>

<span class="c"># 기존 환경 삭제 (확인 후 실행)</span>
<span class="k">if</span> <span class="o">[</span> <span class="nt">-d</span> <span class="s2">"</span><span class="nv">$ENV_PATH</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span>conda <span class="nb">env </span>remove <span class="nt">--prefix</span> <span class="nv">$ENV_PATH</span> <span class="nt">-y</span>
<span class="k">fi</span>

<span class="c"># conda 환경 생성</span>
conda create <span class="nt">-y</span> <span class="nt">--prefix</span> <span class="nv">$ENV_PATH</span> <span class="nv">python</span><span class="o">=</span>3.8.12

<span class="c"># Conda 환경 활성화 (환경 경로 사용)</span>
<span class="nb">source </span>activate <span class="nv">$ENV_PATH</span>

<span class="c"># 패키지 설치</span>
conda <span class="nb">install</span> <span class="nt">-y</span> lightgbm scikit-learn pandas numpy
</code></pre></div></div> <ul> <li> <p><code class="language-plaintext highlighter-rouge">#SBATCH –-time=99:59:59</code> 은 작업 시간 최대 허용치를 의미합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">#SBATCH –-output=testEnv.log</code>의 output log 파일명과 경로, <code class="language-plaintext highlighter-rouge">#SBATCH –-error=testEnv.err</code>의 error log 파일명과 경로를 원하는 데로 변경합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">environment name</code>을 원하는 이름으로 변경합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">python version</code>을 원하는 버전으로 변경합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">conda install -y</code> 뒤에 설치를 원하는 패키지 이름을 입력합니다.</p> </li> </ul> <p>위 내용에 따라 job script를 알맞게 수정하여 <code class="language-plaintext highlighter-rouge">Visual Studio Code</code>에서 작성한 뒤, 클러스터 내 <code class="language-plaintext highlighter-rouge">user home directory</code>에 <code class="language-plaintext highlighter-rouge">[your_env_name].job</code>으로 저장합니다(e.g. <code class="language-plaintext highlighter-rouge">testEnv.job</code>).</p> <h3 id="step-42-작성한-스크립트-실행하기">Step 4.2 작성한 스크립트 실행하기</h3> <p><code class="language-plaintext highlighter-rouge">Visual Studio Code</code> 하단 터미널에</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sbatch [your_env_name].job
</code></pre></div></div> <p>를 입력해 slurm batch job submission을 수행합니다. 작업이 노드에서 성공적으로 실행되면</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Submitted batch job 71
</code></pre></div></div> <p>와 같은 메시지가 뜨고 job 번호가 할당됩니다. 할당되는 job 번호는 나중에 squeue를 통해 정보를 확인하거나 job을 취소할 때 이용되므로 기록해 놓아야 합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>squeue
</code></pre></div></div> <p>커맨드를 통해 작업 실행 현황을 확인할 수 있습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JOBID PARTITION     NAME     USER ST       TIME  NODES NODELIST(REASON)
   71      jobs  testEnv testuser  R       0:15      1 hpc
</code></pre></div></div> <p>또는 아래 커맨드를 통해 실시간(10초 단위)으로 작업 실행 현황을 확인할 수 있습니다. ctrl+c로 escape 할 수 있습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>watch -n 10 -d squeue
</code></pre></div></div> <p>다음 커맨드를 통해 output log, error log파일의 내용을 확인할 수 있습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cat [your_job-name].log
cat [your_job-name].err
</code></pre></div></div> <p>log 파일의 내용을 다음 커맨드를 통해 실시간으로 확인할 수 있습니다. ctrl+c로 escape할 수 있습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tail -f [your_job_name].log
tail -f [your_job_name].err
</code></pre></div></div> <p>터미널을 여러 개 띄워 놓고 각각에 smap과 tail 커맨드를 입력하면 편리하게 실행 상황을 모니터링할 수 있습니다.</p> <p>위 샘플 코드는 약 3분 안에 작업이 완료됩니다. smap에서 작업 목록이 사라진 후 cat으로 로그 파일을 열어서</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Downloading and Extracting Packages: ...working... done
Preparing transaction: - \ done
Verifying transaction: / - \ | / - \ | / - \ done
Executing transaction: / - \ | done
</code></pre></div></div> <p>와 같은 기록이 남아 있으면 conda environment 생성이 완료된 것입니다.</p> <p>작업이 끝나기 전에 취소하려면 scancel 커맨드를 사용합니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>scancel [job_number]
</code></pre></div></div> <p>가상환경을 생성한 후에 패키지를 추가로 설치할 때는 위의 job script에서 가상환경을 삭제하고 다시 만드는 부분</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 기존 환경 삭제 (확인 후 실행)</span>
<span class="k">if</span> <span class="o">[</span> <span class="nt">-d</span> <span class="s2">"</span><span class="nv">$ENV_PATH</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span>conda <span class="nb">env </span>remove <span class="nt">--prefix</span> <span class="nv">$ENV_PATH</span> <span class="nt">-y</span>
<span class="k">fi</span>

<span class="c"># conda 환경 생성</span>
conda create <span class="nt">-y</span> <span class="nt">--prefix</span> <span class="nv">$ENV_PATH</span> <span class="nv">python</span><span class="o">=</span>3.8.12
</code></pre></div></div> <p>을 삭제하고 패키지를 설치하는 커맨드를 추가해 준 다음 job을 제출합니다.</p> <p>가상환경 생성 및 패키지 설치 중 일어나는 오류 중 상당수가 Python에서 기본으로 제공하는 패키지를 설치하려 하거나, 설치할 패키지의 이름을 잘못 입력했기 때문에 발생합니다.</p> <h2 id="step-5-slrum-batch-script-작성하여-서버에-제출하기">Step 5. Slrum batch script 작성하여 서버에 제출하기</h2> <h3 id="step-51-python-코드-작성">Step 5.1 Python 코드 작성</h3> <p>이제 클러스터에서 실행할 Python 코드를 local에서 작성합니다. 코드가 오류 없이 작동하는지 local에서 확인합니다. 그 후 코드 파일을 <code class="language-plaintext highlighter-rouge">user home directory</code>에 옮기거나, <code class="language-plaintext highlighter-rouge">Visual Studio Code</code>내에서 작성하여 저장합니다.</p> <p>아래는 tree 기반 boosting 알고리즘인 LightGBM으로 mnist dataset을 분류하는 코드입니다. Boosting round를 10회 수행하고 학습 결과를 csv파일로 저장합니다. Batch script를 작성할 때는 알고리즘의 output이 자동으로 저장되지 않으므로 파일로 결과를 저장하는 코드를 포함하는 것이 좋습니다. 단, 콘솔에 출력되는 내용은 output log에 자동으로 기록됩니다. 아래 코드를 <code class="language-plaintext highlighter-rouge">python_test_hpc.py</code>로 저장하여 <code class="language-plaintext highlighter-rouge">user home directory</code>에 둡니다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">time</span> <span class="kn">import</span> <span class="n">process_time</span>
<span class="kn">from</span> <span class="n">lightgbm</span> <span class="kn">import</span> <span class="n">LGBMClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.metrics</span> <span class="kn">import</span> <span class="n">accuracy_score</span><span class="p">,</span> <span class="n">log_loss</span>
<span class="kn">from</span> <span class="n">sklearn.datasets</span> <span class="kn">import</span> <span class="n">fetch_openml</span>
<span class="kn">from</span> <span class="n">sklearn.model_selection</span> <span class="kn">import</span> <span class="n">train_test_split</span>


<span class="k">def</span> <span class="nf">lgb</span><span class="p">(</span><span class="n">n</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> <span class="n">c</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">sequence</span><span class="o">=</span><span class="mi">1</span><span class="p">):</span>

    <span class="n">mnist</span> <span class="o">=</span> <span class="nf">fetch_openml</span><span class="p">(</span><span class="sh">'</span><span class="s">mnist_784</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">x_train</span><span class="p">,</span> <span class="n">x_test</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">y_test</span> <span class="o">=</span> <span class="nf">train_test_split</span><span class="p">(</span><span class="n">mnist</span><span class="p">.</span><span class="n">data</span><span class="p">,</span> <span class="n">mnist</span><span class="p">.</span><span class="n">target</span><span class="p">,</span> <span class="n">test_size</span><span class="o">=</span><span class="mf">0.33</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>



    <span class="n">proba_test</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">((</span><span class="n">n</span><span class="p">,</span> <span class="n">y_test</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="nf">len</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">unique</span><span class="p">(</span><span class="n">y_test</span><span class="p">))))</span>
    <span class="n">proba_train</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">((</span><span class="n">n</span><span class="p">,</span> <span class="n">y_train</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="nf">len</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">unique</span><span class="p">(</span><span class="n">y_train</span><span class="p">))))</span>

    <span class="n">test_score</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">train_score</span> <span class="o">=</span> <span class="p">[]</span>

    <span class="n">tr_time</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">seq</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="nf">while</span><span class="p">(</span><span class="n">n</span><span class="p">):</span>

        <span class="n">model</span> <span class="o">=</span> <span class="nc">LGBMClassifier</span><span class="p">(</span><span class="n">n_estimators</span><span class="o">=</span><span class="n">sequence</span><span class="p">)</span>

        <span class="n">t0</span> <span class="o">=</span> <span class="nf">process_time</span><span class="p">()</span>
        <span class="n">model</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">x_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>
        <span class="n">tr_time</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">process_time</span><span class="p">()</span> <span class="o">-</span> <span class="n">t0</span><span class="p">)</span>
        <span class="n">test_score</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">accuracy_score</span><span class="p">(</span><span class="n">y_test</span><span class="p">,</span> <span class="n">model</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">x_test</span><span class="p">)))</span>
        <span class="n">train_score</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">accuracy_score</span><span class="p">(</span><span class="n">y_train</span><span class="p">,</span> <span class="n">model</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">x_train</span><span class="p">)))</span>
        <span class="n">proba_test</span><span class="p">[</span><span class="n">c</span><span class="p">,</span> <span class="p">]</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">predict_proba</span><span class="p">(</span><span class="n">x_test</span><span class="p">)</span>
        <span class="n">proba_train</span><span class="p">[</span><span class="n">c</span><span class="p">,</span> <span class="p">]</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">predict_proba</span><span class="p">(</span><span class="n">x_train</span><span class="p">)</span>

        <span class="n">seq</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">sequence</span><span class="p">)</span>
        <span class="n">sequence</span> <span class="o">*=</span> <span class="mi">2</span>
        <span class="n">n</span> <span class="o">-=</span> <span class="mi">1</span>
        <span class="n">c</span> <span class="o">+=</span> <span class="mi">1</span>

        <span class="n">ce_train</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">ce_test</span> <span class="o">=</span> <span class="p">[]</span>

    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">10</span><span class="p">):</span>
        <span class="n">ce_test</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">log_loss</span><span class="p">(</span><span class="n">y_test</span><span class="p">,</span> <span class="n">proba_test</span><span class="p">[</span><span class="n">i</span><span class="p">]))</span>
        <span class="n">ce_train</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">log_loss</span><span class="p">(</span><span class="n">y_train</span><span class="p">,</span> <span class="n">proba_train</span><span class="p">[</span><span class="n">i</span><span class="p">]))</span>

        <span class="n">np</span><span class="p">.</span><span class="nf">savetxt</span><span class="p">(</span><span class="sh">'</span><span class="s">round</span><span class="sh">'</span><span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">i</span><span class="p">)</span> <span class="o">+</span> <span class="sh">'</span><span class="s">proba_test.csv</span><span class="sh">'</span><span class="p">,</span> <span class="n">proba_test</span><span class="p">[</span><span class="n">i</span><span class="p">])</span>
        <span class="n">np</span><span class="p">.</span><span class="nf">savetxt</span><span class="p">(</span><span class="sh">'</span><span class="s">round</span><span class="sh">'</span><span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">i</span><span class="p">)</span> <span class="o">+</span> <span class="sh">'</span><span class="s">proba_train.csv</span><span class="sh">'</span><span class="p">,</span> <span class="n">proba_train</span><span class="p">[</span><span class="n">i</span><span class="p">])</span>

    <span class="n">np</span><span class="p">.</span><span class="nf">savetxt</span><span class="p">(</span><span class="sh">'</span><span class="s">test_score.csv</span><span class="sh">'</span><span class="p">,</span> <span class="n">test_score</span><span class="p">,</span> <span class="n">delimiter</span><span class="o">=</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">np</span><span class="p">.</span><span class="nf">savetxt</span><span class="p">(</span><span class="sh">'</span><span class="s">train_score.csv</span><span class="sh">'</span><span class="p">,</span> <span class="n">train_score</span><span class="p">,</span> <span class="n">delimiter</span><span class="o">=</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">)</span>

    <span class="n">np</span><span class="p">.</span><span class="nf">savetxt</span><span class="p">(</span><span class="sh">'</span><span class="s">ce_test.csv</span><span class="sh">'</span><span class="p">,</span> <span class="n">ce_test</span><span class="p">,</span> <span class="n">delimiter</span><span class="o">=</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">np</span><span class="p">.</span><span class="nf">savetxt</span><span class="p">(</span><span class="sh">'</span><span class="s">ce_train.csv</span><span class="sh">'</span><span class="p">,</span> <span class="n">ce_train</span><span class="p">,</span> <span class="n">delimiter</span><span class="o">=</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">)</span>


<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">'</span><span class="s">__main__</span><span class="sh">'</span><span class="p">:</span>
    <span class="nf">lgb</span><span class="p">()</span>
</code></pre></div></div> <h3 id="step-52-현재-클러스터-자원-사용량-확인">Step 5.2 현재 클러스터 자원 사용량 확인</h3> <p>아래 커맨드를 통해 <code class="language-plaintext highlighter-rouge">hpc</code> 노드의 cpu와 RAM 사용 현황을 볼 수 있습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sinfo -o "%n %e %m %a %c %C"
</code></pre></div></div> <p>아래와 같은 결과가 나옵니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HOSTNAMES FREE_MEM MEMORY AVAIL CPUS CPUS(A/I/O/T)
hpc 216217 257613 up 64 0/64/0/64
</code></pre></div></div> <ul> <li> <p>CPUS의 A/I/O/T는 allocated/idle/other/total을 의미합니다.</p> </li> <li> <p>자신의 job이 바로 실행되기를 원한다면, Slurm batch script를 작성할 때</p> <ul> <li> <p>RAM 용량을 FREE_MEM보다 적게 설정해야 합니다.</p> </li> <li> <p>CPU 코어 개수를 CPUS idle보다 적게 설정해야 합니다.</p> </li> <li> <p>실제 필요 이상의 자원을 올리면 다른 사용자의 사용에 어려움이 있을 수 있으니 반드시 필요한 만큼만 올려야 합니다.</p> </li> </ul> </li> <li> <p>현재 가용 자원보다 더 많은 자원을 요구하는 script를 작성하면, job이 바로 실행되지 않습니다. 대기 상태에 있다가 다른 user들의 job이 끝나고 자원이 반환되면 job이 실행됩니다.</p> </li> </ul> <h3 id="step-53-slurm-batch-script-작성">Step 5.3 Slurm batch script 작성</h3> <p>앞선 단계에서 만든 해당 <code class="language-plaintext highlighter-rouge">conda environment</code>를 activate하고 코드를 실행하는 Slurm batch script를 작성합니다. 클러스터 소개 페이지의 slurm job configurator를 사용하면 script를 쉽게 작성할 수 있습니다.</p> <p><img src="/assets/img/img23.jpg" alt="이미지23"/></p> <ul> <li> <p><code class="language-plaintext highlighter-rouge">Conda activate</code>에 체크합니다.</p> </li> <li> <p>빈칸들을 채웁니다. 사용 시간을 넉넉하게 입력할 것을 권장합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">Script</code>란에 <code class="language-plaintext highlighter-rouge">python xxx.py</code>라고 작성합니다. 이는 <code class="language-plaintext highlighter-rouge">home directory</code>에 있는 <code class="language-plaintext highlighter-rouge">xxx.py</code> 파일을 Python으로 실행하라는 의미입니다. Job script를 작성하거나 <code class="language-plaintext highlighter-rouge">sbatch</code> 명령어를 사용할 때, <code class="language-plaintext highlighter-rouge">visual studio code</code>의 explorer에서 파일명을 마우스 우클릭하고 경로 복사를 사용하면 편리합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">Print &amp; Copy</code> 버튼을 누르면 내용이 클립보드에 복사됩니다. Slurm batch script의 내용은 아래와 같습니다.</p> </li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#</span>
<span class="c">#SBATCH --job-name=python_test_hpc</span>
<span class="c">#SBATCH --partition=jobs</span>
<span class="c">#SBATCH --account=testuser</span>
<span class="c">#SBATCH --mem=4gb</span>
<span class="c">#SBATCH --ntasks=1</span>
<span class="c">#SBATCH --cpus-per-task=4</span>
<span class="c">#SBATCH --time=01:00:00</span>
<span class="c">#SBATCH --output=/data/home/testuser/python_test_hpc.log</span>
<span class="c">#SBATCH --error=/data/home/testuser/python_test_hpc.err</span>
<span class="c">#SBATCH --nodelist=hpc</span>

<span class="c"># Set up conda environment</span>
<span class="nv">CONDA_ROOT</span><span class="o">=</span>/conda/anaconda3
<span class="nv">CONDA_BIN_PATH</span><span class="o">=</span><span class="nv">$CONDA_ROOT</span>/bin
<span class="nv">ENV_NAME</span><span class="o">=</span>testEnv
<span class="nv">ENV_PATH</span><span class="o">=</span>/data/home/<span class="si">$(</span><span class="nb">whoami</span><span class="si">)</span>/.conda/envs/<span class="nv">$ENV_NAME</span>
<span class="nb">source</span> <span class="nv">$CONDA_BIN_PATH</span>/activate <span class="nv">$ENV_PATH</span>

python python_test_hpc.py
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">python_test_hpc.job</code>이라는 이름으로 클러스터의 <code class="language-plaintext highlighter-rouge">user home directory</code>에 저장합니다.</p> <p>Script 윗부분의 #SBATCH 옵션들의 의미는 다음과 같습니다.</p> <ul> <li> <p><code class="language-plaintext highlighter-rouge">--job-name</code>: 수행할 작업의 이름</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--mem</code>: memory limit</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--nodelist</code>: 작업할 노드의 이름</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--ntasks</code>: 작업의 수</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--cpus-per-task</code>: 각 작업에서 사용할 cpu 코어의 수</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--time</code>: 작업 제한시간</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--account</code>: 해당 작업을 수행하는 계정의 이름</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--partition</code>: group of nodes with specific characteristics</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--nodelist</code>: 사용할 node의 이름</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--output</code>: 코드 실행 결과 log 파일. 확장자는 out이나 log가 가능합니다.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">--error</code>: 코드 실행 결과 log error 파일과 log 파일의 파일명과 저장 경로는 원하는 데로 수정할 수 있습니다. sbatch에 대한 더 자세한 정보는 <a href="https://slurm.schedmd.com/sbatch.html">Slurm 공식 웹페이지</a>를 참조하세요.</p> </li> </ul> <h3 id="step-54-slurm-batch-script-실행">Step 5.4 Slurm batch script 실행</h3> <p><code class="language-plaintext highlighter-rouge">Conda environment</code>를 만들 때처럼, <code class="language-plaintext highlighter-rouge">sbatch</code> 커맨드를 통해 job을 제출합니다. 할당되는 job 번호는 나중에 <code class="language-plaintext highlighter-rouge">squeue</code>를 통해 정보를 확인하거나 job을 취소할 때 이용되므로 기록해 놓아야 합니다.</p> <p>Step 4에서처럼, <code class="language-plaintext highlighter-rouge">ctrl+shift+~</code>를 눌러 터미널을 여러 개 띄우고 <code class="language-plaintext highlighter-rouge">watch -n 10 -d squeue</code>로 작업 현황을 확인하고, <code class="language-plaintext highlighter-rouge">tail -f xxx.out</code>, <code class="language-plaintext highlighter-rouge">tail -f xxx.err</code>으로 콘솔 출력이나 error를 확인합니다. 작업은 4분 정도 걸립니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sbatch python_test_cpu.job
</code></pre></div></div> <p>현재 작업이 자원을 얼마나 할당받았는지 확인하려면 다음 커맨드를 사용합니다. NumCPUs=4가 코어를 4개 할당받았다는 뜻이고, mem=4G가 RAM을 4gb 할당받았다는 뜻입니다. 이 커맨드는 다른 user가 제출한 job에 대해서도 사용할 수 있습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>scontrol show job [job number]
</code></pre></div></div> <p>위 커맨드 실행 결과는 다음과 같습니다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JobId=93 JobName=python_test_hpc
   UserId=testuser(1006) GroupId=testuser(1006) MCS_label=N/A
   Priority=4294901723 Nice=0 Account=testuser QOS=normal
   JobState=RUNNING Reason=None Dependency=(null)
   Requeue=1 Restarts=0 BatchFlag=1 Reboot=0 ExitCode=0:0
   RunTime=00:00:30 TimeLimit=01:00:00 TimeMin=N/A
   SubmitTime=2025-03-11T23:52:38 EligibleTime=2025-03-11T23:52:38
   AccrueTime=2025-03-11T23:52:38
   StartTime=2025-03-11T23:52:38 EndTime=2025-03-12T00:52:38 Deadline=N/A
   SuspendTime=None SecsPreSuspend=0 LastSchedEval=2025-03-11T23:52:38
   Partition=jobs AllocNode:Sid=hpc:410653
   ReqNodeList=hpc ExcNodeList=(null)
   NodeList=hpc
   BatchHost=hpc
   NumNodes=1 NumCPUs=4 NumTasks=1 CPUs/Task=4 ReqB:S:C:T=0:0:*:*
   TRES=cpu=4,node=1,billing=4
   Socks/Node=* NtasksPerN:B:S:C=0:0:*:* CoreSpec=*
   MinCPUsNode=4 MinMemoryNode=4G MinTmpDiskNode=0
   Features=(null) DelayBoot=00:00:00
   OverSubscribe=OK Contiguous=0 Licenses=(null) Network=(null)
   Command=/data/home/testuser/python_test_hpc.job
   WorkDir=/data/home/testuser
   StdErr=/data/home/testuser/python_test_hpc.err
   StdIn=/dev/null
   StdOut=/data/home/testuser/python_test_hpc.log
   Power=
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">Visual Stuio Code</code>의 file explorer는 실시간으로 변화가 반영되지 않습니다. 새로고침 버튼을 눌러 주면 변화가 반영되고 output 파일이 explorer에 보입니다.</p> <h1 id="더-알아보기">더 알아보기</h1> <p><a href="https://doc.zih.tu-dresden.de/jobs_and_resources/slurm_examples/">SLURM Job Examples</a></p>]]></content><author><name></name></author><category term="SSH"/><category term="Python"/><summary type="html"><![CDATA[SSH 접속 방법 및 HPC에서 Python 실행 방법]]></summary></entry><entry><title type="html">Introduction</title><link href="https://yonseiserver.github.io/blog/2025/introduction/" rel="alternate" type="text/html" title="Introduction"/><published>2025-03-02T12:00:00+00:00</published><updated>2025-03-02T12:00:00+00:00</updated><id>https://yonseiserver.github.io/blog/2025/introduction</id><content type="html" xml:base="https://yonseiserver.github.io/blog/2025/introduction/"><![CDATA[<h2 id="1-hpc-node">1. HPC node</h2> <ul> <li> <p>Name: hpc</p> </li> <li> <p>CPU: INTEL® XEON® GOLD 6526Y CPU @ 2.20GHz 32 cores, 64 threads</p> </li> <li> <p>RAM: SAMSUNG M321R8GA0BB0-CQKZJ (64GB) x 4</p> </li> <li> <p>GPU: NVIDIA GeForce RTX 4090 x 1</p> </li> <li> <p>Storage: 2TB NVMe SSD, 8TB HDD</p> </li> </ul> <h2 id="2-slurm-job-scheduler">2. Slurm job scheduler</h2> <h3 id="job-scheduler">Job scheduler</h3> <p>HPC(High performance computing) 시스템은 개인용 컴퓨터와 달리 여러 user가 <code class="language-plaintext highlighter-rouge">hpc node</code>를 공유하며 사용합니다. 따라서 누구의 작업이 언제 어느 node에서 실행될지 결정해 주어야 합니다. 이러한 역할을 수행하는 것이 job scheduler입니다.</p> <p>Job scheduler를 식당의 웨이터에 비유할 수 있습니다. 식당에 사람이 많으면 줄을 서서 기다려야 합니다. 웨이터는 각 손님 그룹의 수에 맞는 자리가 나면 그 그룹을 테이블로 안내합니다.</p> <p><img src="/assets/img/img0.jpg" width="900" height="600"/> </p> <p><strong>용어</strong></p> <ul> <li> <p>Job: user가 클러스터에서 실행하고자 하는 코드(bash, python, R 등을 모두 포함)</p> </li> <li> <p>Batch job submission: user가 미리 작성한 코드(output을 파일로 저장하는 내용 포함)를 scheduler에게 제출하여 non-interactive하게 서버에서 실행하는 것</p> </li> </ul> <h3 id="slurm">Slurm</h3> <p>Slurm(Simple Linux Utility for Resource Management)은 HPC에서 많이 채용하는 job scheduler입니다. 전 세계 TOP 500 슈퍼컴퓨터 중 60%가 slurm을 사용합니다.</p> <p>Slurm은 각 user의 cpu사용량 등 다양한 통계를 기반으로 작업의 우선순위를 결정할 수 있습니다. 예를 들어, University of Toronto의 Computer Science Department의 HPC는 사용 CPU 코어 수 * 사용 시간(초) 값이 낮은 user의 job을 먼저 실행합니다. RAM 사용량은 0.25GB당 CPU 1코어 사용으로, GPU 사용량은 GPU 1개당 CPU 16코어 사용으로 환산합니다.</p> <p>현재 Yonseistat HPC는 위와 같은 점수제가 아닌 FIFO(First In, First Out) 규칙을 사용하고 있습니다. 일정 기간 운영해본 뒤 상황에 맞추어 규칙을 변경할 예정입니다.</p> <h2 id="3-conda-environment">3. Conda environment</h2> <h3 id="virtual-environment">Virtual environment</h3> <p>Virtual environment를 사용하면 한 컴퓨터 내에서 각 user가 독립적으로 Python 패키지를 관리할 수 있습니다. 또한, 여러 컴퓨터에서 동일한 환경을 구축하여 패키지 버전 차이로 인한 문제 발생을 방지할 수 있습니다. 이는 특히 GPU driver, CUDA, cuDNN, 딥러닝 라이브러리의 버전 간 호환성이 중요한 딥러닝 job에서 유용합니다.</p> <p>여러 user가 node를 공유하는 HPC에서 virtual environment의 사용은 필수적입니다. 각 user는 자신의 로컬 컴퓨터와 동일한 Python 환경을 node 내에 구축합니다. 그리고 로컬 컴퓨터에서 코드를 작성하고 이상 없이 실행되는지 테스트합니다. 문제 없이 실행되는 것이 확인된 코드를 slurm을 통해 클러스터에서 실행합니다. Virtual environment는 로컬에서 작성한 코드가 클러스터에서 문제 없이 작동하는 것을 보장합니다.</p> <h3 id="conda-environment">Conda environment</h3> <p>Yonseistat HPC는 <code class="language-plaintext highlighter-rouge">conda</code>를 이용해 virtual environment를 구현합니다. <code class="language-plaintext highlighter-rouge">conda</code>는 Windows, MacOS, Linux를 모두 지원합니다.</p> <p>Yonseistat HPC에서 Python job을 실행하기 위해서는 반드시 conda environment를 사용해야 합니다.</p>]]></content><author><name></name></author><category term="HPC"/><category term="Slurm"/><summary type="html"><![CDATA[HPC Cluster와 Slurm job scheduler에 대한 설명 문서]]></summary></entry></feed>